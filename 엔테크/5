SELECT P.ID, P.NAME, SUM(S.PRICE)
FROM PLACES P, SCHEDULES S
WHERE P.ID(+) = S.PLACE_ID 
    AND  S.SCHEDULED_AT LIKE '2019-01-06%' OR S.SCHEDULED_AT LIKE '2019-01-07%'
HAVING SUM(S.PRICE)<=400
GROUP BY P.ID
ORDER BY P.ID